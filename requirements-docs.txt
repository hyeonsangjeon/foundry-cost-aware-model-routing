# Docs site toolchain (github.io). Isolated from the runtime package deps.
# Build locally:  mkdocs build --strict
# Serve locally:  mkdocs serve
#
# Pinned toolchain for the i18n scaffold (03F). Floors match the versions the
# byte-identical production build was validated against:
#   mkdocs-material 9.7.6, mkdocs-static-i18n 1.3.1, mkdocs-redirects 1.2.3.
mkdocs-material>=9.5
mkdocs-static-i18n>=1.3
mkdocs-redirects>=1.2
